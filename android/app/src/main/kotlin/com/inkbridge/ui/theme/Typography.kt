package com.inkbridge.ui.theme

import androidx.compose.material3.Typography

/**
 * InkBridge typography — Material3 defaults, no custom fonts.
 * Custom fonts are deferred until the design stabilises.
 */
val InkBridgeTypography = Typography()
